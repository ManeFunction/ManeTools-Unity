using UnityEngine;

namespace Mane.Unity
{
    /// <summary>
    /// Makes the serialized field read-only in the inspector.
    /// </summary>
    public sealed class ReadOnlyAttribute : PropertyAttribute { }
}
